class Account {
    // Account model to represent user data
    constructor(username, password) {
        this.username = username; // User's username
        this.password = password; // User's password
    }
}

module.exports = Account; // Export the Account class
