<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <link rel="stylesheet" href="/styles.css">

</head>
<body>

  <h1>Weekly Planner</h1>
  <a href="/account/logout">Logout</a>  

  <table border="1">
      <thead>
          <tr>
              <th>Monday</th>
              <th>Tuesday</th>
              <th>Wednesday</th>
              <th>Thursday</th>
              <th>Friday</th>
              <th>Saturday</th>
              <th>Sunday</th>
          </tr>
      </thead>
      <tbody>
          <tr>
              <% ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'].forEach(day => { %>
                  <td>
                      <h3><%= day %></h3>
                      <% if (goals[day]) { %>
                          <ul>
                              <% goals[day].forEach(goal => { %>
                                  <li><%= goal.type %>: <%= goal.text %></li>
                              <% }) %>
                          </ul>
                      <% } else { %>
                          <p>No goals</p>
                      <% } %>
                  </td>
              <% }) %>
          </tr>
      </tbody>
  </table>
  <a href="/planner/addGoal">Add Goal</a>
  <a href="/planner/updateGoal">Update Goal</a>
  <a href="/planner/deleteGoal">Delete Goal</a>
</body>
</html>